<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.houqixuan.playdate">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".registration"
            android:label="@string/title_activity_registration"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".yourdogs"
            android:label="@string/title_activity_yourdogs"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".adddog"
            android:label="@string/title_activity_adddog"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".findadate"
            android:label="@string/title_activity_findadate"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".findadatelist"
            android:label="@string/title_activity_findadatelist"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".dogpage"
            android:label="@string/title_activity_dogpage"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".filter"
            android:label="@string/title_activity_filter"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".activityDeails"
            android:label="@string/title_activity_activity_deails"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".listrequests"
            android:label="@string/title_activity_listrequests"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".upcomingActivities"
            android:label="@string/title_activity_upcoming_activities"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             ATTENTION: This was auto-generated to add Google Play services to your project for
             App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".oldmatches"
            android:label="@string/title_activity_oldmatches"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>
